namespace Rusty.Serialization
{
    /// <summary>
    /// The formats with built-in support in UCS.
    /// </summary>
    public enum Format
    {
        Cscd,
        Json,
        Xml,
        Csv
    }
}